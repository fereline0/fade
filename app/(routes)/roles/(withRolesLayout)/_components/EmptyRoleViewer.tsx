import { Card, CardBody } from "@heroui/card";

export default function EmptyRoleViewer() {
  return (
    <Card>
      <CardBody>
        <p>Select a role to interact with her</p>
      </CardBody>
    </Card>
  );
}
